application:
  contact:
    email: contact@jsl-group.com
  email-topic: license

management:
  endpoints:
    web:
      exposure:
        include: "*"
    enabled-by-default: true

server:
  port: 8072